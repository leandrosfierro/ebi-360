export default function AdminPage() {
    return (
        <div>
            <h2>PAGE SUPER ADMIN LOADED</h2>
            <p>Si ves esto, entonces el problema es la conexión con Supabase o el sistema de auth en el servidor.</p>
        </div>
    );
}
